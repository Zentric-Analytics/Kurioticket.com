# 19. Testing and quality

## Test categories observed in repo

- TypeScript checks for web and mobile.
- Mobile unit/integration tests and release-pipeline tests.
- Migration validation tests in CI.
- Security checks and secret scans in `security.yml`.

## Known inherited quality debt (from recent approval history)

- Some failing platform/build/runtime tests are inherited and non-blocking to this doc task.
- Full re-baseline requires platform reruns and CI report refresh.

## Current evaluation for this handbook task

- Documentation edits only: non-blocking.
- No runtime code behavior changed.
