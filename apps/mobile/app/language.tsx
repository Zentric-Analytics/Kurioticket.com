import { router } from "expo-router";import { Pressable,StyleSheet,Text,View } from "react-native";import { SafeAreaView } from "react-native-safe-area-context";import { useAppTheme } from "../src/theme/AppTheme";import { FlowIcon } from "../src/features/flow/FlowIcon";import { useMobileLocalization } from "../src/localization/MobileLocalizationProvider";import { mobileLocales } from "../src/localization/mobileLocalizationCatalog";
export default function LanguageScreen(){const{theme}=useAppTheme();const{locale,setLocale,t}=useMobileLocalization();return <SafeAreaView style={[s.safe,{backgroundColor:theme.background}]}><View style={s.header}><Pressable accessibilityRole="button" accessibilityLabel="Go back" onPress={()=>router.back()} style={s.touch}><FlowIcon name="back" color={theme.icon}/></Pressable><Text accessibilityRole="header" style={[s.title,{color:theme.text}]}>{t("language")}</Text><View style={s.touch}/></View><View style={[s.card,{backgroundColor:theme.surface,borderColor:theme.border}]}>{mobileLocales.map(option=><Pressable key={option.code} accessibilityRole="radio" accessibilityState={{checked:locale===option.code}} onPress={()=>void setLocale(option.code)} style={[s.row,{borderBottomColor:theme.border}]}><Text style={[s.label,{color:theme.text}]}>{option.label}</Text>{locale===option.code?<FlowIcon name="check" color="#0754F7"/>:null}</Pressable>)}</View></SafeAreaView>}const s=StyleSheet.create({safe:{flex:1},header:{height:62,flexDirection:"row",alignItems:"center"},touch:{width:52,height:52,alignItems:"center",justifyContent:"center"},title:{flex:1,textAlign:"center",fontSize:22,fontWeight:"800"},card:{margin:18,borderWidth:1,borderRadius:14,overflow:"hidden"},row:{minHeight:58,paddingHorizontal:16,flexDirection:"row",alignItems:"center",borderBottomWidth:StyleSheet.hairlineWidth},label:{flex:1,fontWeight:"700"}});
