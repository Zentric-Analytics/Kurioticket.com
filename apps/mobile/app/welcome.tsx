import { Link, router, Stack } from "expo-router";
import { Pressable, StyleSheet, Text, View } from "react-native";
import { DevBadge } from "../src/components/DevBadge"; import { Logo } from "../src/components/Logo"; import { Screen } from "../src/components/Screen"; import { colors } from "../src/theme/tokens";
export default function Welcome() { return <Screen centered><Stack.Screen options={{ headerShown: false }} /><DevBadge /><View style={styles.hero}><Logo /><Text style={styles.headline}>Plan the next trip with a calmer first step.</Text><Text style={styles.copy}>Kurioticket is building a focused mobile experience for flights first, with hotels and cars following later.</Text></View><Pressable style={styles.primary} onPress={() => router.push("/home")}><Text style={styles.primaryText}>Continue</Text></Pressable>{__DEV__ ? <Link href="/connection-status" asChild><Pressable style={styles.secondary}><Text style={styles.secondaryText}>View connection status</Text></Pressable></Link> : null}</Screen>; }
const styles = StyleSheet.create({ hero: { gap: 18, alignItems: "center" }, headline: { color: colors.navy, fontSize: 34, lineHeight: 40, fontWeight: "900", textAlign: "center" }, copy: { color: colors.slate, fontSize: 17, lineHeight: 25, textAlign: "center" }, primary: { minHeight: 54, borderRadius: 18, backgroundColor: colors.blue, alignItems: "center", justifyContent: "center" }, primaryText: { color: "white", fontWeight: "900", fontSize: 16 }, secondary: { minHeight: 50, borderRadius: 18, borderWidth: 1, borderColor: colors.border, alignItems: "center", justifyContent: "center" }, secondaryText: { color: colors.blue, fontWeight: "800" } });
