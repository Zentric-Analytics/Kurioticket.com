import { Link, Stack } from "expo-router";
import { Pressable, StyleSheet, Text, View } from "react-native";
import { DevBadge } from "../src/components/DevBadge"; import { Logo } from "../src/components/Logo"; import { Screen } from "../src/components/Screen"; import { colors } from "../src/theme/tokens";
const cards = [{ title: "Flights", status: "Available", body: "The first Kurioticket mobile journey starts with flight discovery readiness." }, { title: "Hotels", status: "Coming later", body: "Hotel booking remains postponed until the mobile flow is approved." }, { title: "Cars", status: "Coming later", body: "Car rentals will be introduced after the core app shell matures." }];
export default function Home() { return <Screen><Stack.Screen options={{ title: "Home" }} /><DevBadge /><Logo compact /><Text style={styles.title}>Welcome to Kurioticket</Text><Text style={styles.copy}>This Version 1 shell is ready for visual review and backend connection checks.</Text><View style={styles.grid}>{cards.map((card) => <View key={card.title} style={styles.card}><Text style={styles.cardTitle}>{card.title}</Text><Text style={[styles.status, card.status === "Available" ? styles.available : styles.later]}>{card.status}</Text><Text style={styles.cardBody}>{card.body}</Text></View>)}</View><Link href="/connection-status" asChild><Pressable style={styles.button}><Text style={styles.buttonText}>Open Connection Status</Text></Pressable></Link></Screen>; }
const styles = StyleSheet.create({ title: { color: colors.navy, fontSize: 32, fontWeight: "900" }, copy: { color: colors.slate, fontSize: 17, lineHeight: 25 }, grid: { gap: 14 }, card: { borderRadius: 24, backgroundColor: colors.surface, borderColor: colors.border, borderWidth: 1, padding: 18, gap: 8 }, cardTitle: { color: colors.navy, fontSize: 22, fontWeight: "900" }, status: { alignSelf: "flex-start", overflow: "hidden", borderRadius: 999, paddingHorizontal: 10, paddingVertical: 5, fontWeight: "900" }, available: { backgroundColor: "#ECFDF5", color: colors.success }, later: { backgroundColor: "#F1F5F9", color: colors.muted }, cardBody: { color: colors.slate, fontSize: 15, lineHeight: 22 }, button: { minHeight: 54, borderRadius: 18, backgroundColor: colors.navy, alignItems: "center", justifyContent: "center" }, buttonText: { color: "white", fontWeight: "900" } });
