# Local development only. `npm run local:preview` always reuses the Preview
# identity and requires this value explicitly. Release values live in eas.json.
EXPO_PUBLIC_API_BASE_URL=http://localhost:3000
EXPO_PUBLIC_GOOGLE_WEB_CLIENT_ID=
EXPO_PUBLIC_GOOGLE_IOS_CLIENT_ID=
