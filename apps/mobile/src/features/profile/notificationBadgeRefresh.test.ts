import assert from "node:assert/strict";import { readFileSync } from "node:fs";import test from "node:test";
test("Profile refreshes its backend unread count on focus, notification mutations, and app activation",()=>{const source=readFileSync("src/features/profile/ProfileScreen.tsx","utf8");assert.match(source,/useFocusEffect\(load\)/);assert.match(source,/travelApi\.notificationUnreadCount\(\)/);assert.match(source,/subscribeUnreadCountChanged\(load\)/);assert.match(source,/state === "active"/);});test("bell keeps navigation, accessibility, and the 99+ cap",()=>{const source=readFileSync("src/features/profile/ProfileScreen.tsx","utf8");assert.match(source,/unreadCount > 99 \? "99\+"/);assert.match(source,/accessibilityLabel=\{t\("notifications"\)\}/);assert.match(source,/router\.push\("\/notifications"\)/);});
