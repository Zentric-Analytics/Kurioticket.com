import assert from "node:assert/strict";import { readFileSync } from "node:fs";import test from "node:test";
test("Profile refreshes unread count on every focus including return from Notifications",()=>{const source=readFileSync("src/features/profile/ProfileScreen.tsx","utf8");assert.match(source,/useFocusEffect\(load\)/);assert.match(source,/travelApi\.notificationUnreadCount\(\)/);});test("notification mutations complete before navigation back to Profile",()=>{const source=readFileSync("src/features/notifications/NotificationsScreen.tsx","utf8");assert.match(source,/await travelApi\.markNotificationRead/);assert.match(source,/await travelApi\.markAllNotificationsRead/);});
