import { readSession } from "../../storage/sessionStorage";import { travelApi } from "../../api/travelApi";export { buildRecentSearch,recentSearchId } from "./recentSearchModel";import type { RecentInput } from "./recentSearchModel";export async function recordRecentSearchBestEffort(input:RecentInput|null){if(!input||!await readSession().catch(()=>null))return false;try{await travelApi.createRecentSearch(input);return true;}catch{return false;}}
