export type SessionEventValue={token:string;expires:string;user:{id:string;email:string;name?:string|null}}|null;type Listener=(session:SessionEventValue)=>void;const listeners=new Set<Listener>();export function publishSession(session:SessionEventValue){listeners.forEach(listener=>listener(session));}export function subscribeSession(listener:Listener){listeners.add(listener);return()=>{listeners.delete(listener);};}
