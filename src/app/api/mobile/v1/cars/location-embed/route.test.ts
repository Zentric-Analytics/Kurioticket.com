import assert from "node:assert/strict";
import test from "node:test";
import { GET } from "./route";
const endpoint="https://kurioticket.test/api/mobile/v1/cars/location-embed";
const valid=new URLSearchParams({id:"static-car-luxury-eclass",pickupLocation:"Paris Charles de Gaulle Airport",dropoffLocation:"Paris Charles de Gaulle Airport",pickupDate:"2026-09-06",pickupTime:"10:00",dropoffDate:"2026-09-09",dropoffTime:"10:00",driverAge:"30"});
test("cars map wrapper validates the complete search identity",async()=>{assert.equal((await GET(new Request(endpoint))).status,400);const unknown=new URLSearchParams(valid);unknown.set("id","unknown");assert.equal((await GET(new Request(`${endpoint}?${unknown}`))).status,404);});
test("cars map wrapper fails closed without its server key",async()=>{const previous=process.env.NEXT_PUBLIC_GOOGLE_MAPS_EMBED_API_KEY;delete process.env.NEXT_PUBLIC_GOOGLE_MAPS_EMBED_API_KEY;try{assert.equal((await GET(new Request(`${endpoint}?${valid}`))).status,503);}finally{if(previous!==undefined)process.env.NEXT_PUBLIC_GOOGLE_MAPS_EMBED_API_KEY=previous;}});
test("cars map wrapper returns hardened HTML and ignores attempted URL or key injection",async()=>{const previous=process.env.NEXT_PUBLIC_GOOGLE_MAPS_EMBED_API_KEY;process.env.NEXT_PUBLIC_GOOGLE_MAPS_EMBED_API_KEY="fake-test-key";try{valid.set("googleKey","attacker");valid.set("googleUrl","https://evil.test");valid.set("iframeUrl","https://evil.test");const response=await GET(new Request(`${endpoint}?${valid}`));const html=await response.text();assert.equal(response.status,200);assert.match(html,/\/maps\/embed\/v1\/place/);assert.match(html,/Paris\+Charles\+de\+Gaulle\+Airport/);assert.doesNotMatch(html,/evil\.test|attacker/);assert.equal(response.headers.get("referrer-policy"),"strict-origin-when-cross-origin");assert.equal(response.headers.get("x-content-type-options"),"nosniff");assert.match(response.headers.get("content-security-policy")??"",/frame-src https:\/\/www\.google\.com/);}finally{if(previous===undefined)delete process.env.NEXT_PUBLIC_GOOGLE_MAPS_EMBED_API_KEY;else process.env.NEXT_PUBLIC_GOOGLE_MAPS_EMBED_API_KEY=previous;}});
