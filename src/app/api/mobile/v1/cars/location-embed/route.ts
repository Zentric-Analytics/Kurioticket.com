import { buildGoogleCarMapEmbedUrl } from "@/lib/cars/carMap";
import type { CarSearchParams } from "@/lib/cars/types";
import { getCarDetails } from "@/services/travel/carAggregator";

const headers={"Content-Type":"text/html; charset=utf-8","Cache-Control":"no-store","Referrer-Policy":"strict-origin-when-cross-origin","X-Content-Type-Options":"nosniff","Content-Security-Policy":"default-src 'none'; style-src 'unsafe-inline'; frame-src https://www.google.com; base-uri 'none'; form-action 'none'"};
const escapeHtmlAttribute=(value:string)=>value.replace(/[&<>"']/g,c=>({"&":"&amp;","<":"&lt;",">":"&gt;",'"':"&quot;","'":"&#39;"})[c]!);
const required=["id","pickupLocation","dropoffLocation","pickupDate","pickupTime","dropoffDate","dropoffTime","driverAge"] as const;
export async function GET(request:Request){const query=new URL(request.url).searchParams;const values=Object.fromEntries(required.map(key=>[key,query.get(key)?.trim()??""])) as Record<(typeof required)[number],string>;if(required.some(key=>!values[key])||!/^\d{4}-\d{2}-\d{2}$/.test(values.pickupDate)||!/^\d{4}-\d{2}-\d{2}$/.test(values.dropoffDate))return new Response("Valid car search identity is required.",{status:400});const search:CarSearchParams={pickupLocation:values.pickupLocation,dropoffLocation:values.dropoffLocation,pickupDate:values.pickupDate,pickupTime:values.pickupTime,dropoffDate:values.dropoffDate,dropoffTime:values.dropoffTime,driverAge:values.driverAge};const car=await getCarDetails(values.id,search);if(!car)return new Response("Car not found.",{status:404});const embedUrl=buildGoogleCarMapEmbedUrl({pickupLocation:car.pickupLocation,googleMapsEmbedApiKey:process.env.NEXT_PUBLIC_GOOGLE_MAPS_EMBED_API_KEY});if(!embedUrl)return new Response("Map preview unavailable.",{status:503});const html=`<!doctype html><html><head><meta name="viewport" content="width=device-width,initial-scale=1"><style>html,body{margin:0;width:100%;height:100%;overflow:hidden}iframe{width:100%;height:100%;border:0}</style></head><body><iframe src="${escapeHtmlAttribute(embedUrl)}" title="Car pickup map" referrerpolicy="strict-origin-when-cross-origin" allowfullscreen></iframe></body></html>`;return new Response(html,{status:200,headers});}
