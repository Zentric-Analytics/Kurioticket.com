import assert from "node:assert/strict"; import test from "node:test"; import { createActivityHandler } from "./route"; const req=()=>new Request("https://test");
test("activity requires authentication, scopes owner and returns customer-safe events",async()=>{assert.equal((await createActivityHandler({authenticate:async()=>null,list:async()=>assert.fail()})(req())).status,401);let user="";const events=[{id:"e",type:"SIGN_IN" as const,occurredAt:new Date(2),deviceLabel:null}];const body=await(await createActivityHandler({authenticate:async () => ({ id: "current", user: { id: "owner", email: "owner@example.com" } }),list:async id=>(user=id,events)})(req())).json();assert.equal(user,"owner");assert.equal(body.events[0].occurredAt,new Date(2).toISOString());for(const key of ["metadata","ipAddress","userAgent","payload"])assert.equal(key in body.events[0],false);});
test("activity supports empty lists and safe failures",async()=>{assert.deepEqual(await(await createActivityHandler({authenticate:async () => ({ id: "current", user: { id: "owner", email: "owner@example.com" } }),list:async()=>[]})(req())).json(),{events:[]});const r=await createActivityHandler({authenticate:async () => ({ id: "current", user: { id: "owner", email: "owner@example.com" } }),list:async()=>{throw new Error("secret")}})(req());assert.equal(r.status,503);assert.doesNotMatch(await r.text(),/secret/);});
