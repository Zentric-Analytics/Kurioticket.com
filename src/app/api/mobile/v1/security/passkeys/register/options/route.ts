import { NextResponse } from "next/server"; import { requireMobileSecurity,mobileUnauthorized } from "@/lib/mobile-security-route"; import { registrationOptions } from "@/services/passkeyService";
export async function POST(request:Request){const s=await requireMobileSecurity(request);if(!s)return mobileUnauthorized();const body=await request.json().catch(()=>({}));try{return NextResponse.json({options:await registrationOptions({userId:s.user.id,email:s.user.email,name:s.user.name,accountSessionId:s.id},body.reauthToken)});}catch(e){return NextResponse.json({error:e instanceof Error&&e.message==="inactive"?"Reactivate your account before adding passkeys.":"Verify your account before setting up a passkey."},{status:403});}}
