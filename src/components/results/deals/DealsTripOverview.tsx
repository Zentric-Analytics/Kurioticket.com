import Link from "next/link";
import { BedDouble, Car, Plane } from "lucide-react";
import type { DealsSearch } from "@/lib/deals/dealsSearchParams";
import { buildDealsModifyUrl, getIncludedProducts } from "@/lib/deals/dealsSearchParams";
import { getOverviewData } from "@/lib/deals/dealsResultsPresentation";

export function DealsTripOverview({ search, locale, t, modeLabel }: { search: DealsSearch; locale: string; t: (key: string) => string; modeLabel: string }) {
  const included = getIncludedProducts(search.mode); const data = getOverviewData(search, locale);
  const summaries = [included.flight && { icon: <Plane key="i" aria-hidden />, label: t("flights"), title: data.flight.title, detail: `${data.flight.dates} · ${data.flight.travelers} ${t(data.flight.travelers === 1 ? "deals.results.traveler" : "deals.results.travelers")} · ${t(`deals.cabin.${data.flight.cabin}`)}` }, included.hotel && { icon: <BedDouble key="i" aria-hidden />, label: t("hotels"), title: data.hotel.title, detail: `${data.hotel.dates} · ${data.hotel.nights ?? "–"} ${t(data.hotel.nights === 1 ? "deals.results.night" : "deals.results.nights")} · ${data.hotel.guests} ${t("deals.results.guests")} · ${data.hotel.rooms} ${t("deals.results.rooms")}` }, included.car && { icon: <Car key="i" aria-hidden />, label: t("cars"), title: data.car.title, detail: `${data.car.dates} · ${data.car.pickupTime} – ${data.car.returnTime}` }].filter(Boolean) as { icon: React.ReactNode; label: string; title: string; detail: string }[];
  return <header className="rounded-3xl bg-[#021C2B] p-5 text-white sm:p-7"><div className="flex flex-col justify-between gap-4 sm:flex-row sm:items-start"><div><p className="text-xs font-bold uppercase tracking-wider text-sky-200">{modeLabel}</p><h1 className="mt-1 text-2xl font-extrabold sm:text-3xl">{t("deals.results.tripOptionsTitle")}</h1><p className="mt-2 max-w-3xl text-sm leading-6 text-slate-200">{t("deals.results.tripOptionsExplanation")}</p></div><Link href={buildDealsModifyUrl(search)} className="shrink-0 rounded-xl bg-white px-5 py-3 text-center text-sm font-extrabold text-[#004BB8] focus-visible:outline focus-visible:outline-2 focus-visible:outline-offset-4">{t("deals.results.modify")}</Link></div><div className={`mt-5 grid gap-3 ${summaries.length > 1 ? "md:grid-cols-2" : ""} ${summaries.length > 2 ? "xl:grid-cols-3" : ""}`}>{summaries.map((item) => <div key={item.label} className="rounded-2xl bg-white/10 p-4"><p className="flex items-center gap-2 text-xs font-bold uppercase text-sky-200">{item.icon}{item.label}</p><p dir={item.label === t("flights") ? "ltr" : undefined} className="mt-2 font-extrabold">{item.title}</p><p className="mt-1 text-sm text-slate-200">{item.detail}</p></div>)}</div><p className="mt-4 text-xs text-slate-300">{t("deals.results.separateBooking")}</p></header>;
}
