import test from "node:test";
import assert from "node:assert/strict";
import { readFile } from "node:fs/promises";

test("guided and standalone Flights reuse FlightResultsClient in a presentation mode", async () => { const [client, stage] = await Promise.all([readFile(new URL("../FlightResultsClient.tsx", import.meta.url), "utf8"), readFile(new URL("./DealsFlightResultsStage.tsx", import.meta.url), "utf8")]); assert.match(client, /presentationMode\?: FlightResultsPresentationMode/); assert.match(client, /data-flight-results-experience="deals-guided"/); assert.match(stage, /<FlightResultsClient/); assert.doesNotMatch(stage, /replaceDealsFlightSelection|writeDealsStagedJourneyPlan|writeDealsTripPlan|removeDealsStagedJourneyPlan|markDealsProviderOpened/); });

test("guided Flight results suppress standalone-only UI and provider exits", async () => { const stage = await readFile(new URL("./DealsFlightResultsStage.tsx", import.meta.url), "utf8"); assert.doesNotMatch(stage, /DealsSearchForm|nearbyFare|recentSearch|savedTrip|priceAlert|FaqAccordion|bookingUrl|partnerRedirectUrl|api\/redirect/); assert.match(await readFile(new URL("../FlightResultsClient.tsx", import.meta.url), "utf8"), /showProviderHandoffCopy=\{false\}/); });

test("FlightCard action distinguishes undefined string and null contracts", async () => { const card = await readFile(new URL("../FlightCard.tsx", import.meta.url), "utf8"); assert.match(card, /detailsHref\?: string \| null/); assert.match(card, /detailsHref === undefined \? `\/flights\/details/); assert.match(card, /detailsHref \? <LinkButton/); assert.match(card, /disabled aria-disabled="true"/); });

test("guided shell renders real Flight details and downstream pending states inside the shell", async () => { const shell = await readFile(new URL("./DealsJourneyShell.tsx", import.meta.url), "utf8"); const stage = await readFile(new URL("./DealsFlightDetailsStage.tsx", import.meta.url), "utf8"); const client = await readFile(new URL("../FlightDetailsClient.tsx", import.meta.url), "utf8"); assert.match(shell, /<DealsFlightResultsStage search=\{search\}/); assert.match(shell, /<DealsFlightDetailsStage/); assert.match(shell, /<DealsCarResultsStage/); assert.match(shell, /<DealsReviewStage/); assert.doesNotMatch(shell, /data-deals-guided-flight-details-pending/); assert.match(client, /export function FlightDetailsExperience/); assert.match(stage, /<FlightDetailsExperience/); assert.doesNotMatch(stage, /replaceDealsFlightSelection|writeDealsStagedJourneyPlan|writeDealsTripPlan|removeDealsStagedJourneyPlan|markDealsProviderOpened|api\/redirect/); });
