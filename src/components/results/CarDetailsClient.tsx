import Link from "next/link";
import { ArrowLeft, BriefcaseBusiness, Car, Check, Fuel, MapPin, Snowflake, Star, Users } from "lucide-react";
import { calculateRentalDays, getPrimaryCarOffer } from "@/lib/cars/carResults";
import type { CarSearchParams, NormalizedCarResult } from "@/lib/cars/types";

const humanize = (value: string) => value.replaceAll("-", " ").replace(/\b\w/g, (letter) => letter.toUpperCase());
export function CarDetailsClient({ car, search, resultsHref }: { car: NormalizedCarResult; search: CarSearchParams; resultsHref: string }) {
  const offer = getPrimaryCarOffer(car)!;
  const offers = [...car.offers].sort((a,b) => a.totalPrice-b.totalPrice || a.id.localeCompare(b.id));
  const money = (value:number, currency=offer.currency) => new Intl.NumberFormat("en-US", { style:"currency", currency, maximumFractionDigits:0 }).format(value);
  const days = calculateRentalDays(search.pickupDate, search.dropoffDate);
  const specs = [[Users, `${car.passengers} passengers`], [BriefcaseBusiness, `${car.bags} bags`], [Car, `${car.doors} doors`], [Car, humanize(car.transmission)], [Snowflake, car.airConditioning ? "Air conditioning" : ""], [Fuel, car.mileagePolicy === "unlimited" ? "Unlimited mileage" : `${car.limitedMileageKm} km included`], [Fuel, humanize(car.fuelPolicy)], [MapPin, humanize(car.pickupType)]] as const;
  return <main className="flex-1 bg-[#f6f8fb] pb-28 md:pb-12"><div className="page-shell py-6"><Link href={resultsHref} className="inline-flex items-center gap-2 font-bold text-[#004BB8]"><ArrowLeft size={18} aria-hidden="true" />Back to Cars results</Link>
    <div className="mt-5 grid gap-6 lg:grid-cols-[minmax(0,1fr)_330px]">
      <div className="min-w-0 space-y-5">
        <section className="rounded-xl border border-slate-200 bg-white p-5"><header className="flex flex-wrap items-start justify-between gap-3"><div><p className="text-xs font-bold uppercase tracking-[.14em] text-[#004BB8]">{car.categoryLabel}</p><h1 className="mt-1 text-3xl font-extrabold text-slate-950">{car.modelName} {car.orSimilar && <span className="text-base font-medium text-slate-500">or similar</span>}</h1>{car.supplierRating !== undefined && <p className="mt-2 flex items-center gap-1 text-sm"><Star size={16} className="fill-amber-400 text-amber-400" aria-hidden="true" /><strong>{car.supplierRating.toFixed(1)}</strong>{car.supplierReviewCount !== undefined ? ` (${car.supplierReviewCount} reviews)` : ""}</p>}</div>{car.isDemo && <span className="rounded-full bg-[#eaf2fb] px-3 py-1 text-xs font-bold text-[#004BB8]">Demo vehicle</span>}</header>
          <div className="mt-5 flex min-h-64 items-center justify-center rounded-xl bg-gradient-to-br from-slate-100 to-[#eaf2fb]" role="img" aria-label={car.imageAlt}><Car className="h-36 w-36 text-[#004BB8]/70" strokeWidth={1.1} aria-hidden="true" /></div></section>
        <section className="rounded-xl border border-slate-200 bg-white p-5"><h2 className="text-xl font-bold">Vehicle specifications</h2><div className="mt-4 grid gap-3 sm:grid-cols-2 lg:grid-cols-4">{specs.filter(([,label])=>label).map(([Icon,label])=><div key={label} className="flex items-center gap-2 rounded-lg bg-slate-50 p-3 text-sm font-semibold"><Icon size={18} className="text-[#004BB8]" aria-hidden="true" />{label}</div>)}</div></section>
        <section className="rounded-xl border border-slate-200 bg-white p-5"><h2 className="text-xl font-bold">Pickup and return</h2><div className="mt-4 grid gap-4 sm:grid-cols-2"><div><h3 className="font-bold">Pickup</h3><p>{car.pickupLocation}</p><p className="text-sm text-slate-600">{search.pickupDate} at {search.pickupTime}</p></div><div><h3 className="font-bold">Return</h3><p>{car.returnLocation}</p><p className="text-sm text-slate-600">{search.dropoffDate} at {search.dropoffTime}</p></div></div><p className="mt-4 text-sm">{humanize(car.pickupType)}{car.shuttleRequired ? " · Shuttle required" : ""}</p>{car.pickupInstructions && <p className="mt-2 text-sm"><strong>Pickup instructions:</strong> {car.pickupInstructions}</p>}</section>
        <section className="grid gap-5 md:grid-cols-2"><div className="rounded-xl border border-slate-200 bg-white p-5"><h2 className="text-xl font-bold">What is included</h2><ul className="mt-3 space-y-2">{car.includedItems.map(item=><li key={item} className="flex gap-2 text-sm"><Check size={17} className="shrink-0 text-emerald-600" aria-hidden="true" />{item}</li>)}</ul></div><div className="rounded-xl border border-slate-200 bg-white p-5"><h2 className="text-xl font-bold">Rental requirements</h2><ul className="mt-3 list-disc space-y-2 ps-5 text-sm">{car.requiredDocuments.map(item=><li key={item}>{item}</li>)}</ul>{car.minimumDriverAge && <p className="mt-3 text-sm"><strong>Minimum driver age:</strong> {car.minimumDriverAge}</p>}</div></section>
        <section className="rounded-xl border border-slate-200 bg-white p-5"><h2 className="text-xl font-bold">Rental conditions</h2><dl className="mt-4 grid gap-3 sm:grid-cols-2"><div><dt className="font-bold">Mileage</dt><dd>{humanize(car.mileagePolicy)}</dd></div><div><dt className="font-bold">Fuel policy</dt><dd>{humanize(car.fuelPolicy)}</dd></div>{car.depositAmount !== undefined && <div><dt className="font-bold">Security deposit</dt><dd>{money(car.depositAmount)}</dd></div>}{car.excessAmount !== undefined && <div><dt className="font-bold">Damage excess</dt><dd>{money(car.excessAmount)}</dd></div>}</dl></section>
        <section className="rounded-xl border border-slate-200 bg-white p-5"><h2 className="text-xl font-bold">Important information</h2><ul className="mt-3 list-disc space-y-2 ps-5 text-sm">{car.importantInformation.map(item=><li key={item}>{item}</li>)}</ul></section>
        <section className="rounded-xl border border-slate-200 bg-white p-5"><h2 className="text-xl font-bold">Available offers</h2><div className="mt-4 space-y-3">{offers.map(item=><article key={item.id} className="rounded-lg border border-slate-200 p-4"><div className="flex flex-col gap-3 sm:flex-row sm:items-center sm:justify-between"><div><p className="font-bold">{item.bookingProviderName}</p><p className="text-sm text-slate-600">Rental company: {item.rentalCompanyName}</p><p className="mt-2 text-sm">{item.freeCancellation ? "Free cancellation" : "Non-refundable"} · {item.payAtPickup ? "Pay at pickup" : "Prepaid"}{item.taxesAndFeesIncluded ? " · Taxes and fees included" : ""}</p></div><div className="text-start sm:text-end"><p className="text-sm">{money(item.pricePerDay,item.currency)} per day</p><p className="text-xl font-extrabold">{money(item.totalPrice,item.currency)} total</p><button disabled title="Illustrative demo offer; live booking is unavailable" className="mt-2 rounded-lg bg-slate-200 px-3 py-2 text-sm font-bold text-slate-600 disabled:cursor-not-allowed">Live deal unavailable in demo mode</button></div></div></article>)}</div></section>
      </div>
      <aside className="hidden lg:block"><div className="sticky top-24 rounded-xl border border-slate-200 bg-white p-5 shadow-sm"><p className="text-sm text-slate-600">{money(offer.pricePerDay)} per day</p><p className="text-3xl font-extrabold">{money(offer.totalPrice)}</p><p className="text-sm text-slate-500">{days} rental {days===1?"day":"days"}</p><div className="mt-4 border-t pt-4 text-sm"><p><strong>Rental company:</strong> {car.rentalCompanyName}</p><p><strong>Booking provider:</strong> {offer.bookingProviderName}</p><p>{offer.freeCancellation ? "Free cancellation" : "Non-refundable"}</p><p>{offer.payAtPickup ? "Pay at pickup" : "Prepaid"}</p></div><button disabled className="mt-5 w-full rounded-lg bg-slate-200 px-4 py-3 font-bold text-slate-600">Live deal unavailable in demo mode</button></div></aside>
    </div></div><div className="fixed inset-x-0 bottom-0 z-30 border-t border-slate-200 bg-white p-3 shadow-[0_-8px_24px_rgba(15,23,42,.1)] lg:hidden"><div className="page-shell flex items-center justify-between gap-3"><div><p className="text-xs text-slate-500">Total for {days} {days===1?"day":"days"}</p><p className="text-xl font-extrabold">{money(offer.totalPrice)}</p></div><button disabled className="rounded-lg bg-slate-200 px-4 py-3 text-sm font-bold text-slate-600">Demo offer only</button></div></div></main>;
}
