"use client";
import { useMemo, useState } from "react";
import { useRouter } from "next/navigation";
import { Bell } from "lucide-react";
import type { CarSearchParams, NormalizedCarResult } from "@/lib/cars/types";
import { getPrimaryCarOffer } from "@/lib/cars/carResults";
import { buildCarPriceAlertPayload } from "@/lib/price-alerts/carPriceAlerts";

export function CarPriceAlertControl({search,results}:{search:CarSearchParams;results:NormalizedCarResult[]}) {
 const router=useRouter(); const currencies=useMemo(()=>[...new Set(results.map(getPrimaryCarOffer).filter((offer)=>offer&&Number.isFinite(offer.totalPrice)&&offer.totalPrice>0&&/^[A-Z]{3}$/.test(offer.currency.toUpperCase())).map((offer)=>offer!.currency.toUpperCase()))],[results]);
 const [open,setOpen]=useState(false),[target,setTarget]=useState(""); const [status,setStatus]=useState<"idle"|"saving"|"saved"|"duplicate"|"error">("idle"); const currency=currencies[0]??""; if(!currency)return null;
 const create=async()=>{const value=Number(target);if(!/^\d+(?:\.\d{1,2})?$/.test(target.trim())||!Number.isFinite(value)||value<=0){setStatus("error");return;}setStatus("saving");const response=await fetch("/api/price-alerts",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify(buildCarPriceAlertPayload(search,value,currency))});if(response.status===401){router.push(`/auth/signin?callbackUrl=${encodeURIComponent(location.pathname+location.search)}`);return;}if(response.status===409){setStatus("duplicate");return;}if(!response.ok){setStatus("error");return;}setStatus("saved");setOpen(false);};
 return <section aria-label="Track rental car prices" data-cars-price-alert className="order-2 w-full rounded-xl border border-blue-100 bg-white p-3 shadow-[0_8px_24px_-22px_rgba(15,23,42,0.5)] sm:order-3 sm:rounded-2xl sm:p-4 sm:shadow-sm"><div className="flex items-center justify-between gap-3"><div className="flex min-w-0 items-center gap-2.5"><span className="inline-flex h-8 w-8 shrink-0 items-center justify-center rounded-full bg-blue-50 text-[#004BB8]"><Bell className="h-4 w-4" aria-hidden="true"/></span><div className="min-w-0"><h2 className="truncate text-sm font-bold text-slate-950 sm:text-base">Track rental car prices</h2><p className="mt-1 hidden text-sm text-slate-600 sm:block">Get notified when the lowest total for this complete rental search reaches your target.</p></div></div><button type="button" onClick={()=>{setStatus("idle");setOpen(true)}} className="min-h-11 shrink-0 rounded-xl bg-[#004BB8] px-4 text-sm font-bold text-white">Create price alert</button></div>{open?<div className="mt-3 flex flex-wrap items-end gap-3 border-t border-slate-100 pt-3"><label className="text-sm font-semibold text-slate-700">Target rental total ({currency})<input autoFocus inputMode="decimal" value={target} onChange={e=>{setTarget(e.target.value);setStatus("idle")}} className="mt-1 block min-h-11 rounded-lg border border-slate-300 px-3"/></label><button disabled={status==="saving"} onClick={()=>void create()} className="min-h-11 rounded-lg bg-[#004BB8] px-4 text-sm font-bold text-white">{status==="saving"?"Saving…":"Save alert"}</button><button onClick={()=>setOpen(false)} className="min-h-11 px-3 text-sm font-semibold">Cancel</button></div>:null}{status==="saved"?<p role="status" className="mt-3 text-sm font-semibold text-emerald-700">Rental car price alert created.</p>:null}{status==="duplicate"?<p role="status" className="mt-3 text-sm font-semibold text-amber-800">This rental car alert already exists. Manage it in Price alerts.</p>:null}{status==="error"?<p role="alert" className="mt-3 text-sm font-semibold text-red-700">Enter a valid target total and try again.</p>:null}</section>;
}
