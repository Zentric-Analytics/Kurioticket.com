import assert from "node:assert/strict";import test from "node:test";import { assertAllowedOrigin } from "./passkeys";
const original={...process.env};test.afterEach(()=>{process.env={...original};});
test("keeps approved HTTPS origin and rejects arbitrary origins",()=>{process.env.NEXTAUTH_URL="https://kurioticket.com";process.env.WEBAUTHN_ORIGINS="https://kurioticket.com";assert.doesNotThrow(()=>assertAllowedOrigin("https://kurioticket.com"));assert.throws(()=>assertAllowedOrigin("custom://kurioticket"));assert.throws(()=>assertAllowedOrigin("android:apk-key-hash:*"));});
test("allows only configured Android certificate and package",()=>{const fp=Array(32).fill("AB").join(":");const hash=Buffer.from(fp.replace(/:/g,""),"hex").toString("base64url");process.env.WEBAUTHN_ANDROID_CERT_SHA256=fp;process.env.WEBAUTHN_ANDROID_PACKAGE_NAME="com.kurioticket.app.preview";assert.doesNotThrow(()=>assertAllowedOrigin(`android:apk-key-hash:${hash}`,"com.kurioticket.app.preview"));assert.throws(()=>assertAllowedOrigin("android:apk-key-hash:unapproved","com.kurioticket.app.preview"));assert.throws(()=>assertAllowedOrigin(`android:apk-key-hash:${hash}`,"com.attacker.app"));});
