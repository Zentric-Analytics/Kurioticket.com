import test from "node:test";import assert from "node:assert/strict";import {classifyCars,classifyFlights,classifyHotels} from "./searchContract";
test("authoritative sources and truthful actions",()=>{const flight=classifyFlights([{id:"f",provider:"Duffel"} as never],[],"r");assert.equal(flight.source,"duffel");assert.equal(flight.results[0].searchPolicy.source,"duffel");const hotel=classifyHotels([{id:"h",provider:"Kurioticket static catalogue"} as never],[],"r");assert.equal(hotel.source,"kurioticket-static-hotels");assert.equal(hotel.results[0].searchPolicy.bookable,false);assert.equal(hotel.results[0].searchPolicy.action.kind,"internal-detail");const car=classifyCars([{id:"c",offers:[]} as never],{pickupLocation:"LAX",dropoffLocation:"LAX",pickupDate:"2027-01-01",dropoffDate:"2027-01-02",pickupTime:"10:00",dropoffTime:"10:00",driverAge:"30"},"r");assert.equal(car.source,"kurioticket-static-cars");assert.equal(car.results[0].searchPolicy.bookable,false);assert.equal(car.results[0].searchPolicy.action.kind,"internal-detail");assert.deepEqual(car.warnings,[]);});
