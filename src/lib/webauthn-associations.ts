const csv=(value:string|undefined)=>value?.split(",").map(v=>v.trim()).filter(Boolean)??[];
export function appleAssociation(input=process.env){const apps=csv(input.WEBAUTHN_IOS_APP_IDS);if(!apps.length||apps.some(v=>!/^[A-Z0-9]{10}\.[A-Za-z0-9.-]+$/.test(v)))throw new Error("WEBAUTHN_IOS_APP_IDS is missing or invalid.");return {webcredentials:{apps}};}
export function androidAssociation(input=process.env){const packageName=input.WEBAUTHN_ANDROID_PACKAGE_NAME?.trim();const fingerprints=csv(input.WEBAUTHN_ANDROID_CERT_SHA256);if(!packageName||!/^[A-Za-z][A-Za-z0-9_.]+$/.test(packageName)||!fingerprints.length||fingerprints.some(v=>!/^([0-9A-Fa-f]{2}:){31}[0-9A-Fa-f]{2}$/.test(v)))throw new Error("Android WebAuthn association is missing or invalid.");return [{relation:["delegate_permission/common.get_login_creds"],target:{namespace:"android_app",package_name:packageName,sha256_cert_fingerprints:fingerprints.map(v=>v.toUpperCase())}}];}
